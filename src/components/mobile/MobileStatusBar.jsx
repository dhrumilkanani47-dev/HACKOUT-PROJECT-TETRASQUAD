import React from 'react';

/**
 * Mobile status bar placeholder (disabled to prevent overlapping with native device status bar).
 */
export const MobileStatusBar = () => {
  return null;
};

export default MobileStatusBar;

